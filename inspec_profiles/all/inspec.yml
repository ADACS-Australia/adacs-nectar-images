name: ADACS_IMAGE_TESTS

title: ADACS-Astro image tests
summary: InSpec tests for testing the software installed on the ADACS-Astro image
version: 0.1.0

supports:
  platform: os      # Note: 'os'=openstack

inputs:
  - name: conda_packages
    type: Array
    required: true
