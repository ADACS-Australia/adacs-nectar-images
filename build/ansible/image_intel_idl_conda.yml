---

- name: Intel + IDL + conda image
  hosts: default
  gather_facts: yes
  become: no

  vars_files:
    - vars/volume_mounting.yml

  roles:
    - apt
    - bindings
    - bashrc
    - intel
    - idl
    - conda
    - cleanup
