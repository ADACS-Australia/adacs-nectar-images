---
- name: Intel + IDL + conda image
  hosts: default
  gather_facts: true
  become: false

  vars_files:
    - vars/volume_mounting.yml

  roles:
    - apt
    - inputrc
    - intel
    - idl
    - conda
    - cleanup
