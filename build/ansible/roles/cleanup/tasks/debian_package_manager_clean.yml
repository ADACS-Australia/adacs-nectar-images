---
- name: apt autoclean and autoremove
  become: true
  apt:
    autoclean: true
    autoremove: true

- name: apt clean all
  command: "{{ item }}"
  become: true
  args:
    warn: false
  with_items:
    - apt-get -y clean all
